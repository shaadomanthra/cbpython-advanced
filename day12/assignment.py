'''
    Create a class named Employee with attributes
    id,name,email,role,salary
    methods to access - getName, updateSalary, and changeRole

    create 3 employee objects and set and update different attributes
    ----
    Create a subclass for employee called starPlayer
    with attributes points, level(1,2,3)
    methods getPoints, getLevel, setPoints, setLevel

    create 1 player and dynamically set and get points.
'''